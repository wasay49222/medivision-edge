services:
  edge-node:
    build:
      context: ..
      dockerfile: docker/edge-simulated/Dockerfile
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
    volumes:
      - ../models:/app/models